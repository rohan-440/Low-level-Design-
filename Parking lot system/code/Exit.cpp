#include <bits/stdc++.h>
using namespace std;

#include "ParkingTicket.cpp"

class Exit
{
private:
    int id;

public:
    Exit(int id) : id(id) {}
    void validateTicket(ParkingTicket ticket)
    {
        // logic to validate the ticket
        // e.g., check if the ticket is valid, if the vehicle has exited, etc.
    }
};